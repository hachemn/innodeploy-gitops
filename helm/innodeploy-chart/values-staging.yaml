replicaCount: 2

image:
  repository: nourhenhachem/innodeploy-app
  tag: staging
  pullPolicy: Always

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  className: nginx

  hosts:
    - host: staging.local
      paths:
        - path: /
          pathType: Prefix
